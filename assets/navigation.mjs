import {getRentalFeed,rentalCities} from './rental-feed.mjs';
const header=document.querySelector('.global-header'),triggers=[...document.querySelectorAll('.nav-trigger')];
function closeMenus(focus=false){for(const button of triggers){if(button.getAttribute('aria-expanded')==='true'&&focus)button.focus();button.setAttribute('aria-expanded','false');document.getElementById(button.getAttribute('aria-controls')).hidden=true}}
let citiesReady=false;
async function refreshCities(){if(citiesReady)return;const status=document.querySelector('[data-nav-city-status]');try{const body=await getRentalFeed(),cities=rentalCities(body.data),container=document.querySelector('[data-extra-cities]');const links=cities.map(city=>{const a=document.createElement('a');a.href='/properties/?city='+encodeURIComponent(city);a.textContent=city;const arrow=document.createElement('span');arrow.textContent='›';arrow.setAttribute('aria-hidden','true');a.append(arrow);return a});container.replaceChildren(...links);status.textContent=cities.length?'':'Explore our core cities or browse all rentals.';citiesReady=true}catch{status.textContent='Live city updates are temporarily unavailable. You can still browse all rentals.'}}
for(const button of triggers){button.addEventListener('click',()=>{const open=button.getAttribute('aria-expanded')==='true';closeMenus();if(!open){button.setAttribute('aria-expanded','true');document.getElementById(button.getAttribute('aria-controls')).hidden=false;if(button.getAttribute('aria-controls')==='locations-menu')refreshCities()}});button.addEventListener('keydown',e=>{if(e.key==='ArrowDown'){e.preventDefault();if(button.getAttribute('aria-expanded')!=='true')button.click();document.getElementById(button.getAttribute('aria-controls')).querySelector('a')?.focus()}})}
document.addEventListener('click',e=>{if(!e.target.closest('.nav-group'))closeMenus()});
document.addEventListener('focusin',e=>{if(!e.target.closest('.nav-group'))closeMenus()});
document.addEventListener('keydown',e=>{if(e.key==='Escape'&&triggers.some(b=>b.getAttribute('aria-expanded')==='true')){closeMenus(true);e.stopImmediatePropagation()}},true);
header?.querySelector('.menu-toggle')?.addEventListener('click',()=>{if(!header.querySelector('.nav').classList.contains('open'))closeMenus()});
header?.querySelectorAll('.nav-panel a').forEach(a=>a.addEventListener('click',()=>closeMenus()));
